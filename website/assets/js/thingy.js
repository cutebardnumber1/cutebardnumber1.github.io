import { init, display, image, Sound, draw, Color, Rect, key, K_LEFT, K_RIGHT } from '/website/pygame.js';

var idkwhatthiswindowshouldbecalled = display.set_mode([500, 500])

var frames = 0

idkwhatthiswindowshouldbecalled.fill(new Color("GREEN"))
while (frames < 100) {
    draw.Rect()
}